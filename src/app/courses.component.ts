import { Component } from '@angular/core';

@Component({
    selector: 'courses',
    template: `
        <button class="btn btn-primary">Save</button>    
    `
    /**Adding or installing bootstrap in angular by typing the command in the terminal "npm install bootstrap --save" 
    and importing it to styles.css by the following syntax "@import "~bootstrap/dist/css/bootstrap.css"" to be used */
})
export class CoursesComponent{
  
}